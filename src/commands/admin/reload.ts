import { SlashCommandBuilder } from "discord.js"; import type { Command } from "../../interfaces/command.js"; import { isAdministrator } from "../../middleware/permission.js"; import { infoEmbed, warningEmbed } from "../../embeds/response.js";
export const reloadCommand: Command = { data: new SlashCommandBuilder().setName("reload").setDescription("Reload command registry"), async execute(i) { if (!isAdministrator(i)) return void await i.reply({ embeds: [warningEmbed("Administrator permission is required.", "Permission Denied")], ephemeral: true }); await i.reply({ embeds: [infoEmbed("Commands are loaded statically; restart the bot after deployment changes.", "Command Reload")], ephemeral: true }); } };
