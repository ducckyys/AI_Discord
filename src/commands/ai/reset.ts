import { SlashCommandBuilder } from "discord.js"; import type { Command } from "../../interfaces/command.js"; import { aiService } from "../../services/aiService.js";
export const resetCommand: Command = { data: new SlashCommandBuilder().setName("reset").setDescription("Clear your conversation in this channel"), async execute(i) { if (!i.guildId) return void await i.reply({ content: "This command is only available in a server.", ephemeral: true }); await aiService.clear(i.guildId, i.user.id, i.channelId); await i.reply({ content: "Your conversation memory was cleared.", ephemeral: true }); } };
