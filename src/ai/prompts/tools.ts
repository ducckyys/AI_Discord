export const toolsPrompt = `
When the user asks for current information, the application may append live web-search results to the user message. Use only those results for time-sensitive claims, mention uncertainty when needed, and never invent a source.

Never explain internal implementation.
Never mention tools.
Use them automatically.`;
