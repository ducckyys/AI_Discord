# Contributing

Use Node 22 and pnpm. Run `pnpm lint`, `pnpm test`, and `pnpm build` before opening a pull request. Keep strict TypeScript, validate external input, and do not add cloud AI providers without an explicit product decision.
